📘 เอกสารสรุปการทำงานระบบ Ironpowder (Recycle) – ฉบับอัปเดตล่าสุดระบบบันทึกข้อมูล Recycle (Ironpowder) ถูกออกแบบมาให้เป็นระบบเฉพาะทาง (Specialized System) ที่แยกตัวออกจากฟอร์มทั่วไป เนื่องจากมีความซับซ้อนของการคำนวณและโครงสร้างข้อมูลที่สูงกว่าปกติ1. ฝั่งหน้าบ้าน (Frontend Architecture)ตำแหน่ง: frontend/src/components/formGen/pages/Recycle/เนื่องจากฟอร์มนี้ตารางเยอะและมีการคำนวณข้ามตาราง เราจึงใช้ Hardcoded Components แทนการใช้ JSON Blueprint วาดหน้าA. องค์ประกอบหลัก (Key Components)Ironpowder_index.tsx (Main Container):เป็นศูนย์กลางควบคุม State ทั้งหมดโดยใช้ useForm และ useFieldArrayสิ่งที่อัปเดต: มีการส่งฟังก์ชัน setValue ลงไปให้ Component ลูก (Summary) เพื่อใช้ซิงค์ค่าตัวเลขกลับเข้ามาในฟอร์มSummary.tsx (The Calculation Engine):หน้าที่: เป็น "สมอง" ในการคำนวณยอดรวมต่างๆ แบบ Real-timeLogic ล่าสุด:ใช้ watch() ดักจับการเปลี่ยนแปลงของข้อมูลในทุกตาราง (Input/Output)คำนวณยอดรวมย่อย (Subtotal) ของแต่ละประเภท (Genmatsu A, Genmatsu B, Film, PE Bag, ฯลฯ)✨ จุดสำคัญ: ใช้ useEffect เพื่อสั่ง setValue ยัดค่าผลลัพธ์การคำนวณทั้งหมด กลับลงไปในตัวแปร formData โดยอัตโนมัติ ทำให้เวลากดบันทึก ค่าตัวเลขเหล่านี้จะติดไปด้วย (ไม่เป็น 0 หรือ undefined เหมือนเมื่อก่อน)2. ฝั่งหลังบ้าน (Backend Logic)Pattern: Controller -> Service -> RepositoryA. Service (ironpowder.service.js)ทำหน้าที่ประมวลผลข้อมูลก่อนบันทึก:Data Extraction (การแงะข้อมูล):รับ JSON ก้อนใหญ่ (formData) มาจากหน้าบ้าน✨ ล่าสุด: ทำการดึงค่า "ยอดรวมรายประเภท" (Subtotals) ที่ Frontend ส่งมา ออกมาเก็บในตัวแปรแยกต่างหาก เช่น totalGenmatsuA, totalFilm, totalCleaning เพื่อเตรียมบันทึกลงคอลัมน์เฉพาะTransaction & Approval:บันทึกข้อมูลลง DB ภายใน Transactionเรียกสร้าง Flow การอนุมัติ โดยยิงไปที่ตารางใหม่ Form_Ironpowder_Approval_FlowB. Repository (ironpowder.repository.js)✨ แก้ไขแล้ว: เปลี่ยนคำสั่ง SQL ในการสร้าง Flow อนุมัติ ให้ Insert ลงตาราง Form_Ironpowder_Approval_Flow แทนตารางกลาง (Gen_Approval_Flow) เพื่อแก้ปัญหา Foreign Key Error3. ฐานข้อมูล (Database Schema) – ฉบับล่าสุดเราได้ปรับโครงสร้างให้แยกเป็นสัดส่วน (Decoupled) เพื่อความยืดหยุ่นและปลอดภัยตารางที่ 1: Form_Ironpowder_Submissions (เก็บเอกสาร)มีการเพิ่มคอลัมน์สำหรับเก็บยอดรวมแยกประเภท เพื่อให้ทำ Report ได้ง่ายดาย:Column NameTypeหน้าที่ironpowder_idINT (PK)รหัสเอกสารlot_noNVARCHARเลข LotstatusNVARCHARสถานะ (Draft/Pending/Approved)total_inputDECIMALน้ำหนักขาเข้าtotal_outputDECIMALน้ำหนักขาออกdiff_weightDECIMALส่วนต่างน้ำหนักtotal_genmatsu_aDECIMAL(ใหม่) ยอดรวม Genmatsu Atotal_genmatsu_bDECIMAL(ใหม่) ยอดรวม Genmatsu Btotal_filmDECIMAL(ใหม่) ยอดรวม Filmtotal_pe_bagDECIMAL(ใหม่) ยอดรวม PE Bagtotal_dust_collectorDECIMAL(ใหม่) ยอดรวม Dusttotal_cleaningDECIMAL(ใหม่) ยอดรวม Cleaningform_data_jsonNVARCHAR(MAX)ข้อมูลตารางย่อยทั้งหมด (JSON)ตารางที่ 2: Form_Ironpowder_Approval_Flow (เก็บเส้นทางอนุมัติ)แยกออกมาจากตารางกลาง เพื่อแก้ปัญหา ID ชนกันColumn Nameหน้าที่flow_idรหัส Flowironpowder_id(FK) เชื่อมกับตาราง Ironpowder เท่านั้นapprover_user_idคนที่ต้องอนุมัติstatusสถานะของขั้นตอนนี้ (Pending/Approved)4. สรุป Flow การทำงานเมื่อกด "Drafted"User กรอกข้อมูล: พิมพ์ตัวเลขในตารางต่างๆFrontend คำนวณ: Summary.tsx คำนวณยอดรวม และใช้ setValue อัปเดตตัวแปรเบื้องหลังทันทีUser กดปุ่ม: ข้อมูลทั้งหมด (รวมถึงยอดคำนวณ) ถูกห่อเป็น JSON ส่งไป BackendBackend รับเรื่อง:Service แงะค่ายอดรวมต่างๆ (total_genmatsu_a, etc.) ออกมาInsert ลงตาราง Form_Ironpowder_Submissions (ลงทั้งคอลัมน์แยก และ JSON)สร้าง Flow อนุมัติลงตาราง Form_Ironpowder_Approval_FlowDatabase: บันทึกสำเร็จ ข้อมูลถูกต้อง ครบถ้วน พร้อมสำหรับทำ Report และการอนุมัติขั้นต่อไปเอกสารนี้ครอบคลุมทุกการเปลี่ยนแปลงล่าสุดที่เราทำครับ สามารถใช้เป็น Reference สำหรับการพัฒนาต่อได้เลย!