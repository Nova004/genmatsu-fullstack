📘 เอกสารสรุปการทำงานระบบ Ironpowder (Recycle) – ฉบับอัปเดตล่าสุด (2026-01-20)

ระบบบันทึกข้อมูล Recycle (Ironpowder) ถูกออกแบบมาให้เป็นระบบเฉพาะทาง (Specialized System) ที่แยกตัวออกจากฟอร์มทั่วไป เนื่องจากมีความซับซ้อนของการคำนวณและโครงสร้างข้อมูลที่สูงกว่าปกติ รวมถึงมี Workflow การอนุมัติที่แยกออกมาเป็นเอกเทศ

---

## 1. ฝั่งหน้าบ้าน (Frontend Architecture)

**ตำแหน่ง:** `frontend/src/components/formGen/pages/Recycle/`

เนื่องจากฟอร์มนี้ตารางเยอะและมีการคำนวณข้ามตาราง เราจึงใช้ **Hardcoded Components** แทนการใช้ JSON Blueprint วาดหน้า

### A. องค์ประกอบหลัก (Key Components)

1.  **Ironpowder_index.tsx (Main Container):**
    *   เป็นศูนย์กลางควบคุม State ทั้งหมดโดยใช้ `useForm` และ `useFieldArray`.
    *   **Logic การ Edit:** ใช้ `useUpdateSubmission` (custom hook) ที่เชื่อมต่อกับ `ironpowderService` เพื่อรองรับการแก้ไขข้อมูล
    *   **Logic การ Resubmit:** มีปุ่ม Resubmit ที่จะเรียก API เพื่อ Reset Approval Flow กลับเป็น Pending.

2.  **Summary.tsx (The Calculation Engine):**
    *   หน้าที่: เป็น "สมอง" ในการคำนวณยอดรวมต่างๆ แบบ Real-time.
    *   **Logic ล่าสุด:** ใช้ `watch()` ดักจับการเปลี่ยนแปลงของข้อมูลในทุกตาราง (Input/Output).
    *   คำนวณยอดรวมย่อย (Subtotal) ของแต่ละประเภท (Genmatsu A, Genmatsu B, Film, PE Bag, ฯลฯ).
    *   ✨ **จุดสำคัญ:** ใช้ `useEffect` เพื่อสั่ง `setValue` ยัดค่าผลลัพธ์การคำนวณทั้งหมด กลับลงไปในตัวแปร `formData` โดยอัตโนมัติ ทำให้เวลากดบันทึก ค่าตัวเลขเหล่านี้จะถูกส่งไป Backend ด้วย (รวมถึงการ Cast Type ให้เป็น Number เพื่อกัน Error).

3.  **ReportHistory_Gen_Recycle.tsx (History Page):**
    *   **ตำแหน่ง:** `frontend/src/pages/Reports/ReportHistory_Gen_Recycle.tsx`
    *   แสดงรายการประวัติการส่งฟอร์ม Recycle.
    *   **Authentication:** แสดงปุ่ม Edit/Delete เฉพาะเจ้าของเอกสาร (`submitted_by` ตรงกับ `user_id`).
    *   **Routing:** ลิงก์ไปยัง `/reports/view/recycle/:id` และ `/reports/edit/recycle/:id`.
    *   **Real-time:** มี Socket.io Listener `refresh_data` เพื่ออัปเดตตารางทันทีที่มีการเปลี่ยนแปลง.

---

## 2. ฝั่งหลังบ้าน (Backend Logic)

**Pattern:** Controller (`approval.controller.js`) -> Service (`ironpowder.service.js`) -> Repository (`ironpowder.repository.js`)

### A. Service (ironpowder.service.js)

ทำหน้าที่ประมวลผลข้อมูลหลัก:

1.  **Data Extraction (การดึงข้อมูล):**
    *   รับ JSON ก้อนใหญ่ (`formData`) มาจากหน้าบ้าน.
    *   ✨ **ล่าสุด:** ดึงค่า "ยอดรวมรายประเภท" (Subtotals) ออกมาแยกเก็บในตัวแปร เช่น `totalGenmatsuA`, `totalFilm` ฯลฯ เพื่อบันทึกลงคอลัมน์ใหม่ใน DB.

2.  **Create / Update Logic:**
    *   Insert/Update ลงตาราง `Form_Ironpowder_Submissions` ผ่าน Transaction.
    *   เรียกสร้าง Flow การอนุมัติลงตาราง `Form_Ironpowder_Approval_Flow`.

3.  **Resubmit Logic (การส่งงานใหม่):**
    *   **เป้าหมาย:** รีเซ็ตกระบวนการอนุมัติ โดยไม่ลบข้อมูล Flow ทิ้ง (เพื่อรักษา Sequence).
    *   **Logic:**
        *   `UPDATE Form_Ironpowder_Submissions`: Set Status = 'Pending', Update Timestamp.
        *   `UPDATE Form_Ironpowder_Approval_Flow`: Set Status = 'Pending', `approver_user_id = NULL`.
        *   `DELETE Form_Ironpowder_Approved_Log`: ลบประวัติการอนุมัติ/Comment ทั้งหมดทิ้ง.

4.  **Fetching Data (การดึงข้อมูล):**
    *   **Submission Details:** Join กับตาราง `AGT_SMART_SY.dbo.agt_member` เพื่อดึงชื่อผู้ส่ง (`submitted_by_name`) มาแสดงแทน ID.
    *   **Pending Level:** ใช้ Subquery คำนวณหา `required_level` ของ Step ที่ยัง Pending อยู่ เพื่อใช้แสดง Red Dot Notification.

### B. Controller (approval.controller.js)

*   **Dynamic Table Selection:** มี Helper Function `getTables` เพื่อเลือกตาราง Flow/Log ให้ถูกตามประเภทฟอร์ม (General vs Ironpowder).
*   **Column Mapping:** รองรับชื่อคอลัมน์ที่ต่างกัน (`submission_id` vs `submissionId`).

---

## 3. ฐานข้อมูล (Database Schema) – ฉบับล่าสุด

เราแยกตารางเป็นสัดส่วน (Decoupled) เพื่อความยืดหยุ่น:

### ตารางที่ 1: Form_Ironpowder_Submissions (เก็บเอกสาร)

*   `submissionId` (PK, INT): รหัสเอกสาร unique.
*   `lot_no` (NVARCHAR(50)): เลข Lot.
*   `status` (NVARCHAR(50)): สถานะ (Drafted/Pending/Approved).
*   `submitted_by` (INT): รหัสผู้ส่ง.
*   `total_genmatsu_a` ... `total_cleaning` (DECIMAL): ยอดรวมแยกประเภท (เพิ่มใหม่).
*   `form_data_json` (NVARCHAR(MAX)): ข้อมูลรายละเอียดทั้งหมดในรูปแบบ JSON.

### ตารางที่ 2: Form_Ironpowder_Approval_Flow (เก็บเส้นทางอนุมัติ)

*   `flow_id` (PK): รหัส Flow.
*   `submissionId` (FK): เชื่อมกับตาราง Ironpowder (ชื่อคอลัมน์เป็น camelCase).
*   `required_level` (INT): Level ที่ต้องอนุมัติ.
*   `sequence` (INT): ลำดับการอนุมัติ.
*   `approver_user_id` (INT, Nullable): คนที่กดอนุมัติ (เป็น NULL เมื่อ Reset).
*   `status` (NVARCHAR(50)): สถานะของ Step นั้นๆ (Pending/Approved).

### ตารางที่ 3: Form_Ironpowder_Approved_Log (เก็บประวัติ)

*   `log_id` (PK): รหัส Log.
*   `submissionId` (FK): เชื่อมกับตาราง Ironpowder.
*   `user_approver_id` (INT): รหัสผู้อนุมัติ.
*   `level` (INT): Level ที่อนุมัติ.
*   `action` (NVARCHAR(50)): Action (Approve/Reject).
*   `comment` (NVARCHAR(MAX)): คอมเมนต์.
*   `created_at` (DATETIME): วันเวลาที่บันทึก.

*   ใช้เก็บ Comment และ Action (Approve/Reject) ของแต่ละคน.
*   จะถูกล้างทิ้งเมื่อมีการ Resubmit.

---

## 4. Workflows สำคัญ

*   **User Resubmit:**
    *   Frontend เรียก API -> Backend Update Flow Status ('Pending') + Clear Log -> Approval Process เริ่มต้นใหม่ที่ Step 1.
*   **Submission:**
    *   Frontend ส่ง JSON -> Backend แตก Data ลง Column + Save JSON -> Create new Approval Flow Rows.
*   **Approval:**
    *   Frontend เรียก API Approve -> Backend Update Flow Status ('Approved') + Check Next Step -> ถ้าครบทุก Step Update Submission Status = 'Approved'.

เอกสารนี้ครอบคลุม Logic และ Structure ล่าสุด (ณ วันที่ 2026-01-20) ครับ