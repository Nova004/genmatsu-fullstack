# 📘 Genmatsu Project Master Documentation

เอกสารฉบับสมบูรณ์สำหรับโครงสร้างระบบและฐานข้อมูล (Database Topology) ของโครงการ Genmatsu
_Created by: Developer Assistant AI_
_Last Updated: 2026-01-30_

---

## 1. Project Overview (ภาพรวมระบบ)

ระบบ **Genmatsu** คือระบบ **Digital Traceability & Report System** สำหรับโรงงานผลิต
ออกแบบมาเพื่อทดแทนการใช้กระดาษ (Paperless) โดยเน้นความถูกต้องของข้อมูล (Data Integrity) และการตรวจสอบย้อนกลับ (Traceability)

### **Core Modules (ระบบหลัก)**

1.  **Production Forms (GEN-A/B)**: บันทึกข้อมูลการผลิตประจำวัน (Daily Report)
2.  **Recycle System**: ระบบจัดการผงเหล็ก (Ironpowder) และการรีไซเคิล
3.  **Approval Workflow**: ระบบอนุมัติเอกสารหลายระดับ (Level 1-3)
4.  **Real-time Dashboard**: แสดงสถานะและแจ้งเตือนผ่าน Socket.IO
5.  **NaCl Formula System**: ระบบคำนวณสูตรน้ำเกลือ
6.  **PDF Generation**: สร้างรายงาน PDF อัตโนมัติคุณภาพสูง

---

## 2. Database Structure (โครงสร้างฐานข้อมูล) 🔥

ระบบใช้ **SQL Server** ฐานข้อมูลเดียวคือ **`AGT_SMART_SY`**
ภายในประกอบด้วยตารางจาก **ระบบเดิม (Legacy)** และระดับ **Genmatsu (New System)**

### � Group 1: Legacy / HR Tables (ตารางเดิมของบริษัท)

_ใช้สำหรับดึงข้อมูลพนักงานและโครงสร้างองค์กร (ห้ามแก้ไขโครงสร้าง)_

| Table Name            | Purpose (หน้าที่และการใช้งาน)                                                                                |
| :-------------------- | :----------------------------------------------------------------------------------------------------------- |
| **Gen_Manu_Member**   | **[Users]** ตาราง User หลัก เก็บ Username, Password, และ **LV_Approvals** (ระดับการอนุมัติ)                  |
| **agt_member**        | **[Employee Info]** รายละเอียดพนักงาน (ชื่อไทย/อังกฤษ, อีเมล, รหัสพนักงาน) มักใช้ Join กับ `Gen_Manu_Member` |
| **agt_position**      | **[Master]** ชื่อตำแหน่งงาน (Position)                                                                       |
| **agt_section**       | **[Master]** ชื่อแผนก (Section)                                                                              |
| **Gen_Approved_log**  | **[Log]** เก็บประวัติการอนุมัติเอกสาร (Shared Log สำหรับหลายระบบในโรงงาน)                                    |
| **Gen_Activity_Logs** | **[Log]** เก็บประวัติการกระทำสำคัญ เช่น การแก้ไข Template หรือ Master Data                                   |

---

### � Group 2: Genmatsu Submission Tables (ตารางฟอร์มการผลิต)

_ตารางหลักที่สร้างขึ้นใหม่สำหรับโปรเจคนี้_

| Table Name               | Purpose (หน้าที่และการใช้งาน)                        | Important Columns                                                                      |
| :----------------------- | :--------------------------------------------------- | :------------------------------------------------------------------------------------- |
| **Form_Submissions**     | **[Header]** เก็บหัวเอกสาร GEN-A/GEN-B               | `submission_id` (PK), `lot_no`, `submitted_by`, `status`, `form_type`                  |
| **Form_Submission_Data** | **[Body]** เก็บเนื้อหาฟอร์มทั้งหมด                   | `form_data_json` (เก็บทุก input), `input_kg`, `output_kg`, `yield_percent`, `moisture` |
| **Gen_Approval_Flow**    | **[Flow]** กำหนดว่าเอกสารใบนี้ต้องผ่าน Level ไหนบ้าง | `sequence`, `required_level`, `status` (Pending/Approved)                              |

---

### � Group 3: Ironpowder / Recycle Tables (ตารางงานรีไซเคิล)

_แยกออกมาต่างหากเพื่อความยืดหยุ่น_

| Table Name                        | Purpose (หน้าที่และการใช้งาน)                    |
| :-------------------------------- | :----------------------------------------------- |
| **Form_Ironpowder_Submissions**   | เก็บข้อมูลฟอร์ม Recycle (Header + Body)          |
| **Form_Ironpowder_Approval_Flow** | ระบบ Flow การอนุมัติเฉพาะของงาน Ironpowder       |
| **Form_Ironpowder_Approved_Log**  | เก็บประวัติการกดอนุมัติ/ตีกลับ ของงาน Ironpowder |

---

### 📌 Group 4: Master Data & Configuration (ข้อมูลค่าคงที่/ตั้งค่า)

| Table Name                 | Purpose (หน้าที่และการใช้งาน)                                               |
| :------------------------- | :-------------------------------------------------------------------------- |
| **Form_Master_Templates**  | เก็บรายชื่อ Template ฟอร์ม (มี versioning)                                  |
| **Form_Master_Items**      | เก็บ Config ของ Input Field ในแต่ละ Template (เก็บเป็น JSON)                |
| **Form_Version_Sets**      | จับกลุ่ม Template ว่ารุ่นการผลิตนี้ (Version นี้) ต้องกรอก Template ไหนบ้าง |
| **Form_Version_Set_Items** | รายการ Template ในแต่ละ Set                                                 |
| **Gen_StandardPlan_MT**    | **[ST Plan]** เก็บค่าเป้าหมายการผลิต (Standard Target) แยกตาม Type          |
| **Gen_NaCl_MT**            | **[NaCl]** ตารางสูตรคำนวณน้ำเกลือ (CG Water / NaCl %)                       |

---

### � Group 5: Other Master Tables (อ้างอิง)

| Table Name                | Purpose (หน้าที่และการใช้งาน)    |
| :------------------------ | :------------------------------- |
| **Form_Master_Machines**  | รายชื่อเครื่องจักร (Machine No.) |
| **Form_Master_Products**  | รายชื่อสินค้า (Product Code)     |
| **Form_Master_Shifts**    | กะการทำงาน (Day/Night)           |
| **Form_Production_Lines** | ไลน์การผลิต (Line 1, Line 2)     |

---

## 3. System Architecture (สถาปัตยกรรมระบบ)

### **Frontend (Vite + React)**

- **Port:** 5173
- **Tech:** React 18, TailwindCSS, Axios, Socket.IO Client
- **Build:** Output เป็น Static Files (`/dist`) ไปวางบน IIS

### **Backend (Node.js + Express)**

- **Port:** 4000
- **Tech:** Express, MSSQL (node-mssql), Socket.IO Server
- **Role:** API Gateway, Business Logic, Database Connector

### **Server Environment (Windows Server)**

- **IIS Proxy:** รับ Request จาก Port 81 (`/genmatsu`) -> ส่งต่อ Port 4000
- **Socket Mode:** `Polling` (HTTP Long-polling) เพื่อความเสถียรสูงสุดบน IIS
- **File Server:** ดึงรูปพนักงานจาก Network Path (`\\192.168.1.68\...`)

---

_เอกสารนี้รวบรวมข้อมูลจากการ Audit Source Code ทั้งหมด ณ วันที่ 30 มกราคม 2026_

---

## 4. Work Flow & Versioning Strategy (กระบวนการทำงานและระบบเวอร์ชัน)

ระบบถูกออกแบบมาให้รองรับการเปลี่ยนแปลงของฟอร์ม (Form Changes) ที่ซับซ้อน โดยให้ความสำคัญกับ **Data Integrity** (ข้อมูลเก่าต้องไม่เพี้ยน) และ **Active Updates** (ข้อมูลใหม่ต้องล่าสุดเสมอ)

### **4.1 Active Versioning & Multi-Template**

1. **Multi-Template Composition:**
   - ฟอร์ม 1 ใบ (เช่น AS2-D) ไม่ได้เกิดจาก Template เดียว แต่เกิดจากการรวมร่างของ **Multiple Templates** (เช่น Header, Material Step, Production Step).
   - แต่ละ Template มี **Version Indipendent** (แยกกันอิสระ).

2. **Active Date Logic:**
   - การดึง "เวอร์ชันล่าสุด" จะใช้เกณฑ์ **Effective Date** (`effective_date <= GETUTCDATE()`)
   - Admin สามารถสร้าง Draft Version ล่วงหน้าได้ โดยตั้ง `effective_date` เป็นอนาคต ระบบจะยังไม่ดึงมาใช้จนกว่าจะถึงเวลา

### **4.2 Submission State Behavior**

พฤติกรรมของระบบจะเปลี่ยนไปตามสถานะของเอกสาร (Status):

| Status                         | Behavior (พฤติกรรม)                                                                                                                                                               |
| :----------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Draft / Rejected / Pending** | **Dynamic Update:** ทุกครั้งที่กด Save/Resubmit ระบบจะคำนวณหา Active Templates ล่าสุดมาสร้าง Version Set ใหม่ทันที (เพื่อให้ User ได้ใช้ฟอร์มเวอร์ชันล่าสุดเสมอ)                  |
| **Approved**                   | **Locked History:** ระบบจะ **ห้าม** เปลี่ยน Version Set เด็ดขาด เพื่อคงสภาพหน้าตาและข้อมูลของฟอร์มให้เหมือนกับตอนที่ถูกอนุมัติ 100% (แม้ว่า Master Template จะเปลี่ยนไปแล้วก็ตาม) |

### **4.3 Integrity Safeguards**

- **Unique Template Filtering:** ระบบมี Logic ป้องกันการใส่ Template ซ้ำซ้อนใน Version Set เดียวกัน
- **Strict Matching:** การค้นหา Version Set เดิมมาใช้ซ้ำ (Reuse) จะใช้การนับจำนวนแถวที่แม่นยำ (`COUNT(*)`) หากพบ Version Set ที่ผิดปกติ (มี Duplicate Items) ระบบจะสร้างอันใหม่ให้ทันทีและทิ้งอันเก่าไป
